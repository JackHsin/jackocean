import styled from 'styled-components';

export const CartIconContainer = styled.div`
    width: 45px;
    height: 45px;
    position: relative;
    display: flex;
    align-items: center;
    justify-content: center;
    cursor: pointer;
`;
CartIconContainer.displayName = 'CartContainer';

export const ShoppingIconContainer = styled.img`
    width: 50px;
    height: 50px;
`;